## What changed?
<!-- If this PR changes editor-visible behavior (completions, hover, diagnostics, go-to-definition, code actions), please add screenshots or examples showing the before and after. -->

## Why?

## How was this tested?
<!-- Which editor(s) did you verify this in, and against which project type (platform, production template, plugin, app, plain Symfony)? -->

## Related issue or discussion

<!--
Before opening this PR:
- Small fixes (typos, docs) can go straight to a PR.
- Larger changes (new LSP features, new language/file-type support, changes to
  existing completion or diagnostic behavior) should be discussed in an issue first.
- Run `go test ./...` and `golangci-lint run ./...` locally.
- Add or update tests for bug fixes and new behavior — including fixtures for any
  new Twig/XML/YAML/PHP parsing paths.
- Note any change to the indexer or its cache format, since it affects startup
  time and may require a re-index for existing users.
- Keep the PR focused — smaller PRs are easier to review and merge.
-->